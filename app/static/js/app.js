/**
 * ME Statistics — Main JavaScript
 * =================================
 * Handles interactive features: sidebar state, language toggle,
 * notification bell, and future Chart.js integration.
 */

document.addEventListener('DOMContentLoaded', () => {
    // Lucide icons initialization (if loaded via CDN)
    if (window.lucide) {
        lucide.createIcons();
    }
});
